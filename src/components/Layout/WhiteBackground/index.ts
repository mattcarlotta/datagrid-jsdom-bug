/* istanbul ignore file */
import styled from "@emotion/styled";

const WhiteBackground = styled.div`
  height: 100vh;
  background: url(${process.env.NEXT_PUBLIC_IMAGEAPI}/images/body_bg.png);
`;

export default WhiteBackground;
